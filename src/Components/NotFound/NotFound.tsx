import React from 'react';
import Grid from '@material-ui/core/Grid';

const Notfound = (): JSX.Element => {
    return (
        <Grid container item>
            <Grid item className="notfound">
                Page Not found
      </Grid>
        </Grid>
    );
};

export default Notfound;
